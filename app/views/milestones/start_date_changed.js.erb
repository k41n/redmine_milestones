<% if @version.present? and @version.start_date.present? and @version.start_date > @newval %>
    if (!confirm('<%= escape_javascript(@oob_warning) %>'))
    {
        $('milestone_start_date').value = '<%= params[:oldval] %>';
    }
<% end %>
